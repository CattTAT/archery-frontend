import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FEAA4B",
      light: "#FEAA4B",
      dark: "#FEAA4B",
      contrastText: "#FFFFFF",
    },
    secondary: {
        main: "#E0E0E0",
        light: "#E0E0E0",
        dark: "#E0E0E0",
        contrastText: "#FFFFFF",
    },
    background: {
      default: "#FFE590",
    },
    
  },
});

export default theme;